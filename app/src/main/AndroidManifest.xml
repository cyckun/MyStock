<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chandler.red.mystock">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.StartingWindowTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.DisplayMessageActivity"
            android:label="@string/title_activity_display_message"
            android:theme="@style/AppTheme.StartingWindowTheme.NoActionBar" />
        <activity
            android:name=".activity.DisplayPriceActivity"
            android:label="@string/title_activity_display_price"
            android:theme="@style/AppTheme.StartingWindowTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".StockImgActivity" />
        <activity
            android:name=".SerachActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/search"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".SerachActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/search"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".activity.Main2Activity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustUnspecified|stateHidden">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:configChanges="orientation|keyboardHidden"
                    android:label="@string/app_name"
                    android:windowSoftInputMode="adjustUnspecified|stateHidden" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ExchangeActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/exchange"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".activity.BuySearchActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/search"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/login" />
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/register" />
        <activity
            android:name=".activity.ForgetPwdActivity"
            android:label="@string/forget_pwd" />
        <activity
            android:name=".activity.ImageShowActivity"
            android:theme="@style/transparent" />
        <activity
            android:name=".activity.MyInfoActivity"
            android:label="@string/my_info" />
        <activity
            android:name=".activity.PhotoSelectActivity"
            android:label="@string/photo_select" />
        <activity
            android:name=".imageloader.ImagePickActivity"
            android:label="@string/photo_select"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".activity.SearchImagesActivity"
            android:label="@string/photo_search"
            android:theme="@style/AppTheme.ActivityTheme" />
        <activity android:name=".activity.FullScreenActivity" />
    </application>

</manifest>